function setCover(){
    page = "cover";
    subpage = null;
    leftContent = {"collapse": true};
    rightContent = {"collapse": true};
    focusContent = {
        "type": "static",
        "data": "This is the mock interface of the TakoAI Business Intelligence Platform."
    };

    viewAll();
}